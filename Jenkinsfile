pipeline {

    agent any

    stages {

        stage("build") {

            steps {
                echo "building the app..."
            }
        }
    }
     stages {

        stage("test") {
            echo "testing the app..."
            steps {

            }
        }
    }
     stages {

        stage("deploy") {
            echo "deploying the app..."
            steps {

            }
        }
    }
}